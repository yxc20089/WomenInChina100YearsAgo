__pycache__/
*.py[cod]
.DS_Store
.pytest_cache/
.venv/
.cache/
*.egg-info/

# Screening derivatives are reproducible and may be hundreds of megabytes.
artifacts/benchmark-pages/images/
artifacts/lossless-pilot/images/
artifacts/gold-pages/
# Annotation packets contain derived archive text and reviewer work; keep them
# local until data-rights and publication review explicitly approve release.
artifacts/gold-packet*/
artifacts/rag-three-year/
artifacts/segmentation-*.json
# Per-job ingestion outputs are content-addressed in PostgreSQL and may contain
# full-resolution page images or model artifacts.
artifacts/ingestion-*/
artifacts/terminal-failure-pilot/
